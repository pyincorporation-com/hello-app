apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: namespaxe-ingress
  namespace: pyinc
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: hello-app.ns.namespaxe.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-app-service
                port:
                  number: 80
